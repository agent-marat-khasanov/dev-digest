---
description: DevDigest project rules
alwaysApply: true
---

Read `AGENTS.md` in the repo root for stack, coding rules, workflow rules, and do-not-touch areas.

Before working in a package (`server/`, `client/`, `reviewer-core/`, `e2e/`), also read that package's `AGENTS.md` and `INSIGHTS.md`.

These files are the single source of truth — follow them exactly.
