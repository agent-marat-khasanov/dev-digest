{
  "eval_id": 5,
  "eval_name": "review-digests-module",
  "prompt": "Review the proposed digests module (fixture: evals/fixtures/digests-module/) strictly for architecture violations and report each one as a structured finding.",
  "assertions": [
    "[5.1][detect] A finding reports the route constructing DigestsRepository and calling it directly, bypassing the service layer",
    "[5.2][detect] A finding reports business logic (severity aggregation / top-files ranking) living in the route",
    "[5.3][detect] A finding reports raw Drizzle rows being returned to HTTP instead of the DigestDto mapping that helpers.ts already provides",
    "[5.4][detect] A finding reports the service calling the mail API with fetch directly (and reading process.env) instead of an adapter behind a port resolved from the Container",
    "[5.6][decoy] No finding flags the concrete repository class / $inferSelect row types — rule-7 allowances, not violations",
    "[5.7][decoy] No finding flags helpers.ts or the Zod contract — they are the correct pattern"
  ]
}