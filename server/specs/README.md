# Server Specs

API-level specifications for `@devdigest/api`.

Add files here as needed:
- `openapi.yaml` — OpenAPI / Swagger spec for all HTTP routes
- `<module>.spec.md` — route contract, request/response examples, error codes

> E2E browser flows live in `../../e2e/specs/`.
