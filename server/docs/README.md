# Server Docs

Module-specific documentation for `@devdigest/api`.

Add files here as the project grows:
- `architecture.md` — server-specific diagrams (DI wiring, module plugin lifecycle)
- `database.md` — schema decisions, migration conventions
- `adapters.md` — how to add a new external service adapter

> Root-level docs live in `../../docs/` (architecture.md, learnings.md, agent-prompts/).
