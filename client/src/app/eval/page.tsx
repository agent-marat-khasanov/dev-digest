import { EvalDashboardView } from "./_components/EvalDashboardView";

/* Route: /eval (sidebar "Eval Dashboard"). Thin route entry — the view,
   styles and helpers are colocated under _components/EvalDashboardView. */
export default function EvalDashboardPage() {
  return <EvalDashboardView />;
}
