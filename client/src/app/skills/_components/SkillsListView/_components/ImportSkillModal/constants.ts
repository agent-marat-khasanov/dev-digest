/** Modal width (px) — wider than Create to fit the preview pane. */
export const MODAL_WIDTH = 820;

/** Accepted file extensions for the picker. Server validates again. */
export const ACCEPT = ".md,.zip,application/zip";
